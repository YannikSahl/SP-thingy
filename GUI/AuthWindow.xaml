<Window x:Class="GUI.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="AuthWindow" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontWeight="Bold" FontSize="15" TextAlignment="Center" Margin="5">Sharepoint Auth</TextBlock>
        <TextBlock Grid.Row="1" TextAlignment="Center"> Name </TextBlock>
        <TextBox Grid.Row="2" Height="40" Margin="5"></TextBox>
        <TextBlock TextAlignment="Center" Grid.Row="3"> Password</TextBlock>
        <PasswordBox Grid.Row="4" Height="40" Margin="5"></PasswordBox>
        <Button Grid.Row="5" Margin="5" Height="40">Verbinden</Button>
        <Button Grid.Row="6" Margin="5" Height="40" Background="#FFF3F3F3" BorderBrush="{x:Null}" Click="OfflineModus_ButtonClick">Offline Modus</Button>
    </Grid>
</Window>
