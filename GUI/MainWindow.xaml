<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
        Title="Hauptformular" Height="450" Width="800" Closing="Window_Closing">
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top" Background="#FF444444" Foreground="White" >
            <MenuItem Header="_Datei" Background="#FF444444">
                <MenuItem Header="_Im-/Export" Foreground="Black">
                    <MenuItem Header="_Auswahl exportieren" Foreground="Black"/>
                    <MenuItem Header="_Importieren" Foreground="Black"/>
                </MenuItem>
                <MenuItem Header="_Neue Abfrage" Foreground="Black" Click="OpenAbfrageWindow"/>
                <MenuItem Header="_Aktualisieren" Foreground="Black"/>
                <Separator />
                <MenuItem Header="_Schließen" Foreground="Black" Click="MenuCloseApp_Click"/>
            </MenuItem>
            <MenuItem Header="_Tools" Background="#FF444444">
                <MenuItem Header="_DB Pflege" Foreground="Black"/>
                <MenuItem Header="_Pfade" Foreground="Black"/>
            </MenuItem>
            <MenuItem Header="Extras" Background="#FF444444">
                <MenuItem Header="Einstellungen" Foreground="Black" Click="OpenSettingsWindow"/>
            </MenuItem>
        </Menu>
        <DockPanel DockPanel.Dock="Bottom" Background="#FF444444" Height="16" LastChildFill="True">
            <TextBlock VerticalAlignment="Center" DockPanel.Dock="Left" Foreground="LightGray" Margin="2,0,2,0" FontSize="10">Verbindung:</TextBlock>
            <TextBlock VerticalAlignment="Center" x:Name="ConnectionStatus" DockPanel.Dock="Left" Foreground="LightGray" Margin="2,0,2,0" FontSize="10"></TextBlock>

            <TextBlock VerticalAlignment="Center" DockPanel.Dock="Left" Foreground="LightGray" Margin="2,0,2,0" FontSize="10">| Bearbeitung:</TextBlock>
            <TextBlock VerticalAlignment="Center" x:Name="EditStatus" DockPanel.Dock="Left" Foreground="LightGray" Margin="2,0,2,0" FontSize="10"></TextBlock>
        </DockPanel>
        <TextBlock x:Name="ConnectionStatusBar" Height="20" DockPanel.Dock="Bottom" Background="#FF21C947" Visibility="Collapsed" TextAlignment="Center">Verbunden</TextBlock>
        <DockPanel>

            <DockPanel DockPanel.Dock="Top" Background="#FFE8E8E8">
                <TextBlock DockPanel.Dock="Left" Padding="5" FontWeight="Bold" Margin="15,0,0,0" VerticalAlignment="Center">Ausgewähltes Dokument (nach PAD):</TextBlock>
                <TextBlock x:Name="SelectedPad" VerticalAlignment="Center" DockPanel.Dock="Left" Padding="5" FontWeight="Bold" FontStyle="Normal" FontSize="16"></TextBlock>
            </DockPanel>

            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="200" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition x:Name="DocumentViewContainer" Width="34" MinWidth="33" MaxWidth="500"/>
                </Grid.ColumnDefinitions>
                <DockPanel Margin="5,0,5,5">
                    <DockPanel DockPanel.Dock="Top" Margin="5,0,5,0">
                        <TextBox x:Name="SearchPpTextfield" Width="200" DockPanel.Dock="Left" VerticalContentAlignment="Center" BorderBrush="{x:Null}" Background="#FFF0F0F0"/>
                        <Button DockPanel.Dock="Left" Padding="5" BorderBrush="{x:Null}" Click="SearchButton_Click">Suchen</Button>
                        <Button DockPanel.Dock="Left" Padding="8,4,8,4" BorderBrush="{x:Null}" Background="#FFE8E8E8" FontWeight="Bold" Click="ClearPpSearchButton_Click">
                            <Button.Foreground>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="#FFFF2020"/>
                                    <GradientStop Color="#FFFF8B18" Offset="1"/>
                                </LinearGradientBrush>
                            </Button.Foreground> X
                        </Button>
                        <Button x:Name="EditModeIcon" IsHitTestVisible="False" DockPanel.Dock="Right" HorizontalAlignment="Right" Width="20"  Padding="5" BorderBrush="{x:Null}">
                            <Button.Background>
                                <ImageBrush ImageSource="/gui_resources/sperren.png" Stretch="Uniform" TileMode="None"/>
                            </Button.Background>
                        </Button>
                        <Button x:Name="EditModeButton" DockPanel.Dock="Right" HorizontalAlignment="Right" Padding="5" BorderBrush="{x:Null}" Click="EditModeButton_Click" Background="#FFDDDDDD">Bearbeitungsansicht</Button>
                    </DockPanel>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition MinHeight="150"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition MinHeight="50"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition MinHeight="50"/>
                        </Grid.RowDefinitions>
                        <DockPanel Grid.Row="0" Margin="0,0,0,5">
                            <TextBlock Text="PP Tabelle" DockPanel.Dock="Top" FontWeight="Bold" Foreground="#FF686868" FontStyle="Italic"></TextBlock>
                            <DataGrid x:Name="PpTable" ItemsSource="{Binding}" RowEditEnding="PP_TABELLE_RowEditEnding" SelectionChanged="PP_TABELLE_SelectionChanged"></DataGrid>
                        </DockPanel>
                        <GridSplitter Grid.Row="1"  Height="5"  HorizontalAlignment="Stretch"/>
                        <DockPanel Grid.Row="2" Margin="0,5,0,5">
                            <TextBlock Text="PL Tabelle" DockPanel.Dock="Top" FontWeight="Bold" Foreground="#FF686868" FontStyle="Italic"></TextBlock>
                            <DataGrid x:Name="PlTable"/>
                        </DockPanel>
                        <TextBlock x:Name="PlTableEmptyMessage" Grid.Row="2" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" FontStyle="Italic" Foreground="#FF4D4D4D" Margin="0,10,0,0">Keine Auswahl</TextBlock>
                        <GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch" />
                        <DockPanel Grid.Row="4" Margin="0,5,0,0">
                            <TextBlock Text="PH Tabelle" DockPanel.Dock="Top" FontWeight="Bold" Foreground="#FF686868" FontStyle="Italic"></TextBlock>
                            <DataGrid x:Name="PhTable"/>
                        </DockPanel>
                        <TextBlock x:Name="PhTableEmptyMessage" Grid.Row="4" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" FontStyle="Italic" Foreground="#FF4D4D4D" Margin="0,10,0,0">Keine Auswahl</TextBlock>
                    </Grid>
                </DockPanel>

                <GridSplitter x:Name="ViewSplitter" Grid.Column="1" Grid.Row="0" Width="5" HorizontalAlignment="Stretch" />

                <Expander Grid.Column="2" Grid.Row="0" Margin="5,0,5,5" ExpandDirection="Left" Collapsed="Expander_Collapsed" Expanded="Expander_Expanded" SizeChanged="Expander_SizeChanged">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Padding="5" FontWeight="Bold">gefundene Dokumente</TextBlock>
                        <ScrollViewer>
                            <StackPanel x:Name="DocumentView"></StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Expander>

            </Grid>
        </DockPanel>
    </DockPanel>
</Window>

